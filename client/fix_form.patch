--- src/components/WaitlistForm.jsx
+++ src/components/WaitlistForm.jsx
@@ -86,7 +86,6 @@
                                aria-expanded={open}
-                               className="premium-trigger premium-trigger-compact"
+                               className="premium-trigger premium-trigger-compact justify-center gap-1.5 px-3"
                        >
-                               <span className="iso-chip" aria-hidden="true">{current.iso}</span>
-                               <span className="text-paper/90">{current.code}</span>
+                               <span className="text-paper/90 font-medium tracking-wide">{current.code}</span>
                                <span
@@ -208,7 +207,7 @@
                                                <span className="field-hint">Optional</span>
                                        </span>
-                                       <div className="grid grid-cols-[8.75rem_minmax(0,1fr)] gap-3 sm:grid-cols-[10rem_minmax(0,1fr)]">
+                                       <div className="grid grid-cols-[5.5rem_minmax(0,1fr)] gap-3 sm:grid-cols-[6rem_minmax(0,1fr)]">
                                                <CountryCodeDropdown
                                                        value={form.countryCode}
                                                        onChange={(v) => setField('countryCode', v)}
                                                />
                                                <input
                                                        type="tel"
                                                        value={form.whatsappNumber}
-                                                       onChange={(e) => setField('whatsappNumber', e.target.value)}
+                                                       onChange={(e) => {
+                                                               const val = e.target.value;
+                                                               if (/^[\d\s\-\+]*$/.test(val)) {
+                                                                       setField('whatsappNumber', val);
+                                                               }
+                                                       }}
+                                                       pattern="[\d\s\-\+]*"
                                                        placeholder="Number"
                                                        autoComplete="tel-national"
                                                        className="underline-input min-w-0"
                                                />
                                        </div>
